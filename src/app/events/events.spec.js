describe( 'Event List', function() {

    beforeEach( module( 'simpleApp.events' ) );

    it( 'should load events', inject( function() {
    expect( true ).toBeTruthy();
    }));

    it( 'should filter events created by user', inject( function() {
    expect( true ).toBeTruthy();
    }));

    it( 'should filter events user is attending', inject( function() {
    expect( true ).toBeTruthy();
    }));
});

